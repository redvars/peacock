---
import DemoComponentCard from '../../../../components/DemoComponentCard.astro';
---
<p class='text-body-large'>Here are different types of inputs.</p>

<DemoComponentCard lang={'html'}>
  <p-number-counter value='4282828'></p-number-counter>

  <script is:inline>
    const $counter = document.querySelector('p-number-counter');

    setInterval(() => {
      $counter.value = $counter.value + parseInt(Math.floor(Math.random() * 1000));
    }, 1000);
  </script>
</DemoComponentCard>
