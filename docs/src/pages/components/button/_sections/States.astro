---
import HighlighterCard from '../../../../components/HighlighterCard.astro';
---
<pc-text type='heading' heading-size='3'>Selected</pc-text>

<pc-text type='body' expressive>Make buttons look active by adding the selected <code>boolean</code> attribute to any
  <code>&lt;button&gt;</code> element.
</pc-text>

<HighlighterCard lang={'html'}>
  <pc-button selected>Primary</pc-button>
</HighlighterCard>

<br />

<pc-text type='heading' heading-size='3'>Disabled</pc-text>
<pc-text type='body' expressive>Make buttons look inactive by adding the disabled <code>boolean</code> attribute to
  any <code>&lt;button&gt;</code> element.
</pc-text>

<HighlighterCard lang={'html'}>
  <pc-button disabled>Primary</pc-button>
  <pc-button variant='outline' disabled>Outline</pc-button>
  <pc-button variant='ghost' disabled>Ghost</pc-button>
</HighlighterCard>
