---
import HighlighterCard from '../../../../components/HighlighterCard.astro';
---

<pc-text type='heading' heading-size='3'>Disabled</pc-text>
<pc-text type='body'
>Make input look inactive by adding the disabled <code class='highlighter-rouge' data-lang='html'>boolean</code>
  attribute to any
  <code class='highlighter-rouge' data-lang='html'>&lt;input&gt;</code> element.
</pc-text>

<HighlighterCard lang={'html'}>
  <pc-toggle disabled>Disabled toggle</pc-toggle>
  <pc-toggle disabled value='true'>Disabled toggle</pc-toggle>
</HighlighterCard>

<pc-text type='heading' heading-size='3'>Readonly</pc-text>
<pc-text type='body'
>Make input look inactive by adding the disabled <code class='highlighter-rouge' data-lang='html'>boolean</code>
  attribute to any
  <code class='highlighter-rouge' data-lang='html'>&lt;input&gt;</code> element.
</pc-text>

<HighlighterCard lang={'html'}>
  <pc-toggle readonly>Readonly toggle</pc-toggle>
  <pc-toggle readonly value='true'>Readonly toggle</pc-toggle>
</HighlighterCard>
