---
import ComponentSectionNav from './ComponentSectionNav.astro';
interface Props {
  sections: any;
  title?: string;
}

const { sections, title } = Astro.props;
---
<div class='component-overview-tab'>
  <div class='component-content'>

    {
      sections.map((section) => (
        <div id={`section-${section.title.toLowerCase().replaceAll(" ", "-").replaceAll("/", "-")}`}
             class='component-section'>
          <h3 class='heading-scrollable text-headline-large'>{section.title}</h3>

          <section.Component/>
        </div>))
    }
  </div>
  <ComponentSectionNav sections={sections} title={title} />
</div>
